# end Spring Boot App

This is a minimal Spring Boot application generated inside the `end/` folder.

Build and run with Maven:

```powershell
cd end
mvn clean package
mvn spring-boot:run
```

Or run the produced jar:

```powershell
java -jar target\end-0.0.1-SNAPSHOT.jar
```

Health check: GET http://localhost:8080/hello
